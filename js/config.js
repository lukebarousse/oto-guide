// Supabase project config. The anon key is safe to publish (it only grants what
// the row-level-security policies allow). Leave blank to run in static mode.
window.OTO_CONFIG = {
  url: "",      // e.g. "https://abcdefgh.supabase.co"
  anonKey: ""
};
